# practices/ — Claude Code 運用指示書(Interactive Practice Lab)

このフォルダは、ベストフィット問題集を原本とした**演習用インタラクティブ Web 教材**を量産する場所です。**単一HTMLファイル**で、PCとスマホ両対応の自走演習ツールを作ります。

ルートの `CLAUDE.md` からここに到達したら、本ファイルの指示に従って作業してください。

---

## 1. このツールの位置づけ

### `lectures/` との違い

| 観点 | lectures(姉妹ツール) | **practices(本ツール)** |
|---|---|---|
| 目的 | 投影 + 復習用スライド | **隙間時間の自走演習** |
| 単位 | スライド遷移 | **1問カード**(ステップ送り) |
| 答えの出し方 | クリックで後出し展開 | **回答入力 → 採点 → 解説 → ビジュアル** |
| 対応する原本 | 学習ノート + 解答PDF | **ベストフィット問題 + 解答(docx)** |
| デザイン | 紺・青系フォーマル | **青系を基調とした爽やかでオシャレなトーン** |
| フォント(明朝系) | Zen Old Mincho | **YuKyokasho Yoko + Klee One**(教科書体) |

### 公開URL

- 演習サイトトップ: `https://mikikof.github.io/mikikof-lab/practices/`
- 単元ページ: `https://mikikof.github.io/mikikof-lab/practices/articles/{番号}-{slug}/`

---

## 2. ディレクトリ構成

```
practices/
├── CLAUDE.md                              本ファイル(制作の方針・哲学)
├── index.html                             演習サイトトップ(単元一覧)
├── _source/                               原本素材。答えの正本
│   ├── ベストフィット問題/                章別 Word(問題編・節ごと)
│   └── ベストフィット解答/                1章ぶんまとめの Word(全問の正答+解説)
├── articles/
│   └── {章}-{節}-{slug}/
│       └── index.html                     各単元の完成品
└── skills/
    └── interactive-practice/
        ├── SKILL.md                       技術仕様(色・型・JS設計・落とし穴)
        ├── components.md                  コピペ可能なパーツカタログ
        └── examples/
            └── 01-03-intellectual-property.html  凍結リファレンス
```

### 単元の命名規則
- 番号は `{章}-{節}` 形式(例: `01-03` = 1章3節)
- スラッグは英語ハイフン区切り(例: `intellectual-property`、`security-and-law`)
- 配下は必ず `index.html`

---

## 3. 制作着手前に必ず読むもの

新しい単元を作る・既存を修正するときは、**以下の順で必ず読んでから**コードに触る:

1. 本ファイル(このCLAUDE.md)— 制作哲学・チェックリスト
2. `skills/interactive-practice/SKILL.md` — 技術仕様・Design Tokens・モバイル UI 機構・落とし穴
3. `skills/interactive-practice/components.md` — 部品カタログ(コピペ元)
4. `skills/interactive-practice/examples/01-01-information-and-media.html` — **最新完成品リファレンス**(モバイル UI 完備)
   - 旧版の `01-03-intellectual-property.html` も同等機構を持つが、新規開発はまず 01-01 をコピーする
5. `_source/ベストフィット問題/` の対応 docx + `_source/ベストフィット解答/` の対応問題範囲 — **問題と答えの正本**

「制作哲学」「技術仕様」「正本」の3つを読まずに書くと、過去のバグや方針ズレを再生産することになります。

---

## 4. 制作哲学 — これを外したらやり直し

### 4.1 原本に完全準拠する
- **問題文は一字一句変えない**(改行・読点・(ア)(イ)などの記号も)
- **正答・選択肢の順序は原本通り**
- 自分の知識で「こうだろう」と書かない。原本で使われている**根拠フレーム**(条文・概念・論じ方)をそのまま採用する
- agent に答えの監査を委譲した場合、「全件 ✓」報告を鵜呑みにしない。**最低でも自分で原本 docx を Read して再確認**する

### 4.2 答えは必ず後出し
- 例題(原本では解答が見えている)も含めて、すべて **タップで解答表示** 形式
- 練習問題は「入力 → 採点ボタン → 解答+解説+ビジュアル」の流れ
- 例題は「解答を見るボタン → 解答+解説+ビジュアル」(入力経由しなくてよい、お手本として)
- 語群・選択肢を表示するときも、正解だけ色付け・太字にしない

### 4.3 解説は必ず付ける(原本に無い場合は独自に補強)
- 原本の解答 docx に解説が全く無い小問・選択肢があっても、**そのまま空欄にしない**
- 各小問の対応・各正答(誤答)の理由を、原本の根拠フレームに沿って自前で補完する
- 「補足」セクションを別立てしてもよい(原本にあったオリジナル解説と区別する)
- これを破ると「答えだけ出る学習体験」になり、教材の価値が落ちる

### 4.4 ビジュアル(ポンチ図・インフォグラフィクス)を必ず添える
- 各問題の解説後に、その内容を視覚化した図を添える
- 問題タイプに応じたビジュアル:
  - 4種マッチング → 4色カードグリッド
  - ツリー構造 → SVG family tree
  - 比較 → 2列対比ボックス
  - フロー判定 → SVG フローチャート
  - チェックリスト → カード型チェックリスト
- ビジュアルは「テキストの図解」ではなく「**記憶のフック**」として設計する

### 4.5 おさらい(REVIEW)は Q&A インタラクティブ
- 単元の冒頭に置く「基本知識のおさらい」は、**ただの一枚絵では記憶に残らない**
- 各論点を「Q. 〜?」の問いかけ → タップで「A. 〜」のビジュアル + 解説が展開する形式
- カードは**独立トグル**(複数同時に開ける、アコーディオンにしない)
- 6モジュール程度に分けて、単元全体の地図 → 詳細トピック の順に並べる

### 4.6 各問題のページ内リトライ
- 採点後のフィードバック内に「もう一度解く」「次の問題へ」ボタンを自動注入
- ワンタップでその問題だけリセット → 再挑戦可能(全体リセット不要)
- 例題側では「もう一度確認」ラベル(解答を伏せて再確認できる)

### 4.7 問題は手応えを残す
- 子供っぽい誘導(「やってみよう」「頑張ろう」「サクッと」)は禁止
- 採点後のフィードバックは**短く中立**:「正解」「不正解」「全問完答」「部分正解 X / Y」
- ❌「素晴らしい!」「完璧!」「お見事!」「いいね!」
- ✅「正解」「不正解」「完答」

### 4.8 文章トーン
- ルート `CLAUDE.md` の「文章トーン」節を全面適用
- カード説明・サマリ・フッターまで、**理路整然・抑制的・アカデミック**
- 詳細な禁止表現と Before/After は `essays/CLAUDE.md` §7-2/7-6 を参照

### 4.9 デザインは lectures とは差別化(青基調)
- lectures は紺・青系のフォーマル
- **practices は青基調(ペールブルー〜ネイビー)で爽やか・低ストレス・オシャレに**
- 装飾絵文字、過剰なアニメは禁止(❤️🎉🚀 はなし。✓ ○ × ⊕ ⟲ など機能記号は可)
- ハイライトには warm amber を1点、一貫して使う(寒色だけだと冷たすぎる)

詳細な色・フォント仕様は `skills/interactive-practice/SKILL.md` の Design Tokens を見ること。

### 4.10 スマホ実機の体験を必ず作り込む
スマホで触ったときに「リッチで気持ちいい」と感じる仕掛けを、**標準装備として必ず搭載**する。例題追加・新規単元・既存修正どれでも、これらを削ってはいけない。

| 機構 | 期待される挙動 | SKILL.md 参照 |
|------|----------------|---------------|
| **スポットライト v2** | 短冊型カード(`.ip-rich` `.bd-card` `.lk-card` `.cc-cat-item`)タップで `scale(1.18)` PC / `1.12` mobile に拡大 + ゴールド縁取りリング + 画面全体を radial-gradient + backdrop-filter blur で暗転 + 自動で画面中央へスクロール。バックドロップ・Esc・再タップで解除。 | §8.5.1 |
| **haptic フィードバック** | 採点・解答表示・もう一度・次の問題・スポットライト切替・ステージ送りで `navigator.vibrate(6-12ms)`。iOS は grace degrade で無音。 | §8.5.2 / §8.5.5 |
| **3-col → モバイル横スワイプ snap カルーセル** | `.origin-compare.three-col`(inline style 禁止、必ずクラスで指定)。モバイルは 84vw 幅で snap、上に「← 横にスワイプ →」hint、下に IntersectionObserver 連動のドットインジケーター。 | §8.5.3 |
| **サマリスコアのカウントアップ** | サマリ到達時に `summary-grade` `stat-full/partial/skipped/rate` が 0 → 実値へ easeOutCubic でカウントアップ。 | §8.5.4 |
| **水平スクロール領域とスワイプの分離** | `.viz-svg-wrap` `.family-tree-wrap` 内タッチは page-swipe ハンドラを抑制(`isInsideHorizontalScroll`)。新しい横スクロール領域を追加するときは必ずクラスを SPOT_SELECTOR と isInsideHorizontalScroll の closest 引数に追記する。 | §9.4b |
| **採点後スクロールは sticky topbar 直下** | `scrollIntoView({ block: 'center' })` は使わない。`.fb-banner` の rect.top - topbarH - 12 へ手動スクロールし、「正答」が必ず先頭に来るようにする。 | §9.4c |
| **iOS 二重タップズーム遅延の解消** | 主要タップ要素全部に `touch-action: manipulation`。 | §9.4d |
| **タップターゲット 44px 以上** | モバイル時の最小タップ領域。 | §10 |

---

## 5. 技術的な制約

### 必須
- **単一HTMLファイル**で完結
- CSS・JS は全て内包
- Google Fonts のみ外部 CDN として許可
- Safari(macOS / iPad / iPhone)と Chrome で動作

### 禁止
- ❌ フレームワーク(React, Vue, Alpine.js 等)
- ❌ localStorage / sessionStorage(セッション中のみのスコア保持)
- ❌ 重いライブラリ
- ❌ ビルドステップが必要な構成

### ナビゲーション(標準装備)
- 左サイドバーのタイムライン(全環境で開閉可能なドロワー)
- ステップ送り(前 / 採点 / 次 ボタン)
- キーボード(← → / Enter / Space / Esc / M キー)
- スワイプ対応(横60px以上、800ms以内、横スクロール領域内では抑制)
- 進捗インジケータ(現在 / 全問数、完答数)

### モバイルリッチ UI(必須・§4.10 と SKILL.md §8.5)
- スポットライト v2(短冊カードタップで暗転 + 拡大)
- haptic フィードバック(Android のみ実振動、iOS は無音)
- 3-col の origin-compare は `.three-col` クラス → モバイル横スワイプ snap カルーセル + ドットインジケーター
- サマリスコアのカウントアップ
- 水平スクロール SVG とスワイプの分離(`isInsideHorizontalScroll`)
- 採点後スクロールは sticky topbar 直下に固定
- `touch-action: manipulation` で iOS 二重タップ遅延を解消

### ユニバーサル対応
- 同一URLで PC(1100px+) / タブレット(720–1100px) / スマホ(〜720px)
- iPhone セーフエリア対応(`viewport-fit=cover` + `env(safe-area-inset-bottom)`)
- 新規コンポーネント追加時は必ず `@media (max-width: 720px)` を書く
- タップターゲットは最低 44px(モバイル)/ 38px(デスクトップ)

詳細は `skills/interactive-practice/SKILL.md` を見ること。

---

## 6. 標準ワークフロー(新規単元1本)

### [1] 素材の確認
- `_source/ベストフィット問題/` から対応 docx を unzip → `word/document.xml` をパースしてテキスト抽出
- `_source/ベストフィット解答/` の対応章 docx も同様にパース
- **節ごとの問題範囲を特定**(解答 docx は1章まとめなので、対応問題番号でフィルタする)
- 例: 1章03 → 問題は類題15〜17 + 練習18〜22

### [2] 構成案の提示と合意
- カバー範囲、問題数、問題タイプ(single/multi/match/ox/fill/multi_per_sub)を案として出す
- 例題と練習問題で扱いを分ける(例題=お手本ツアー、練習=採点付き)
- ユーザーに合意を取る前にコードを書き始めない

### [3] 実装
- `skills/interactive-practice/examples/01-01-information-and-media.html` をベースにコピー(最新完成品。モバイル UI v2 完備)
- `articles/{番号}-{slug}/index.html` に配置
- カラートークン・タイポグラフィ・SVGテンプレは絶対に変えない
- 問題タイプごとの入力UIは `components.md` から該当パーツをコピーする
- ビジュアル(viz)は `components.md` のインフォグラフィクスカタログから選ぶ
- 3列対比は `<div class="origin-compare three-col">` を使う(inline style 禁止)
- 単元タイトル・slug・問題数を一括置換し、既存の JS フック(spotlight / haptic / animateCounter / isInsideHorizontalScroll / 採点後スクロール / カルーセルドット注入)は**絶対に消さない**

### [4] 答えの照合(原本との突き合わせ) ★ 飛ばすと事故る
- 全問について、**正答・選択肢・解説の根拠フレーム**を原本 docx と1問ずつ照合
- 独自に追加した4択や応用問題は作らない(原本にない問題を勝手に追加しない)
- 確認終了後、ユーザーへの報告に「ベストフィット問題 X〜Y / 解答 docx と全問照合済み」を明記

### [5] 解説の補完
- 原本に解説が無い小問・選択肢に対して、**4.3 のルール**で自前補強したかチェック
- 「補足」セクションをすべての例題と、必要な練習問題に追加

### [6] ビジュアルの追加
- すべての例題・練習問題にビジュアルが付いているか確認
- おさらいセクションが Q&A 形式の6モジュール程度になっているか

### [7] 構文・動作チェック
- HTML 入れ子の検証(`<div class="digest-mod">` などの兄弟関係を保つ)
- JS 構文(`node --check` で extracted script を確認、または HTML パーサで balance チェック)
- 4ブレイクポイント(1100px / 720px / 380px / landscape phone)で崩れないか
- `SKILL.md` の「既知の落とし穴」と照らし合わせる

### [7.5] モバイル UX 動作確認 ★ §4.10 が崩れていないか
- 短冊カードタップで scale 拡大 + 画面暗転 + ゴールドリング が出る
- 3列 compare が `.three-col` クラス指定で、モバイル時は横スワイプ + 下にドット
- 採点ボタン押下 → 「正答」バナーが画面先頭に見える(下に飛んでいない)
- 水平スクロール SVG をスワイプしてもページ遷移しない
- サマリ画面で「X/Y」がカウントアップアニメで増える
- haptic は Android 実機で振動、iOS でエラー出ない

### [8] リリース
- `practices/index.html`(単元一覧)にエントリ追加
- 完成版を `skills/interactive-practice/examples/{番号}-{slug}.html` にコピー(凍結)

---

## 7. 納品前チェックリスト

### 内容
- [ ] 原本の全問が網羅されている(欠落なし、独自追加なし)
- [ ] **正答・解説の根拠フレームを原本 docx と1問ずつ照合済み**
- [ ] 答えはすべて入力後/タップ後に表示(語群・一覧でも正解を先に色付けしていない)
- [ ] 例題3問すべてに「補足」セクションとビジュアルが付いている
- [ ] 練習問題8問すべてに「解説」+ ビジュアルが付いている
- [ ] 原本に無い解説は独自に補完済み(4.3)
- [ ] 禁止表現(「やってみよう」「サクッと」「素晴らしい!」等)が残っていない

### おさらい(REVIEW Digest)
- [ ] Q&A 形式の6モジュール構成(独立トグル、アコーディオンにしない)
- [ ] 各カードがタップで開閉、複数同時に開ける
- [ ] HTML 構造で 6つの `.digest-mod` がすべて **兄弟関係**(入れ子なし)
- [ ] ファミリーツリー(モジュール 01)が単元の地図として機能している

### デザイン・書体
- [ ] 青基調のカラートークンが守られている(`SKILL.md` Design Tokens)
- [ ] 明朝系は YuKyokasho Yoko + Klee One スタックを使っている
- [ ] 本文・連続文に明朝を使っていない(見出し・用語名のみ)
- [ ] 本文 16px 以上、問題文 16px 以上

### ナビゲーション・機能
- [ ] サイドバータイムラインが全環境で開閉可能(PC・タブレット・スマホ)
- [ ] M キー / メニューボタン / 内部の× / 背景タップで開閉
- [ ] 各問題のフィードバックに「もう一度解く / 次の問題へ」ボタンが自動注入される
- [ ] 例題側は「もう一度確認」ラベルになっている
- [ ] 採点後にステータスがタイムラインに反映される(完答/部分/不正解)
- [ ] サマリ画面で正答率と間違い問題リストが見える

### レスポンシブ
- [ ] 1100px / 720px / 380px / landscape phone で崩れない
- [ ] `<meta viewport-fit=cover>` が入っている
- [ ] iPhone ホームインジケータと衝突しない
- [ ] タップターゲットが 44px 以上(モバイル)

### バグ予防(`SKILL.md` の「既知の落とし穴」を全部チェック)
- [ ] `showFeedback` の null safety(score-tag が無くても OK)
- [ ] `digest-mod` の入れ子バグなし(独立した兄弟構造)
- [ ] Enter/Space の二重発火対策(`<button>` には keydown ハンドラを足さない)
- [ ] スワイプとタップの干渉なし(ボタン要素は swipe で除外、水平スクロール領域も `isInsideHorizontalScroll` で除外)
- [ ] 採点後スクロールは sticky topbar 直下(`block: 'center'` を使っていない)
- [ ] 主要タップ要素に `touch-action: manipulation` が当たっている

### モバイルリッチ UI(§4.10 標準装備)
- [ ] 短冊型カード(`.ip-rich` `.bd-card` `.lk-card` `.cc-cat-item`)タップで scale 拡大 + ゴールドリング + 画面暗転 が動く
- [ ] スポットライト解除(再タップ・バックドロップ・Esc)が動く
- [ ] 「タップで解除 ✕」hint がモバイル底部に出る
- [ ] 3列対比は `<div class="origin-compare three-col">` で記述(inline style なし)
- [ ] モバイルで 3列 compare が横スワイプ snap + 下にドットインジケーター
- [ ] サマリ到達時に `summary-grade` `stat-full/partial/skipped/rate` がカウントアップアニメ
- [ ] 採点・解答・もう一度・次・スポットライト・ステージ送り で haptic が呼ばれる

---

## 8. Git 運用

コミットプレフィックスは `practices:` を使う:

```
practices: 01-04-security-and-law を新規追加
practices: 1章03 のCCマーク組み合わせを原本準拠に修正
practices: SKILL.md に新しい viz パターンを追記
```

他ツール(`lectures/` `essays/`)と混在させない。push 先は常に `origin main`。

---

## 9. 困ったとき

- 実装で迷ったら `skills/interactive-practice/examples/` の最新完成品を読む
- 部品が必要なら `skills/interactive-practice/components.md` からコピー
- 設計で迷ったら `skills/interactive-practice/SKILL.md` の落とし穴セクションを照らす
- 問題の正答や解説の根拠で迷ったら独自推論せず、必ず `_source/ベストフィット解答/` の対応箇所を Read で開いて確認
- それでも判断つかない場合は作業を止めてユーザーに確認
