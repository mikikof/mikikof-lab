# 情報Ⅰ_2学期 — 公開除外
#
# mikikof-lab は PUBLIC リポジトリ。ここに追跡した物は GitHub 上で誰でも読める。
# 実体の正本は my-company(private)側の各キットにある。回フォルダは
# `python3 _ops/build_kai.py <回番号>` で組み直せる派生物なので、外しても失われない。
#
# 出荷前に `python3 _ops/check_public.py` を通す（追跡 NG が1件でもあれば push しない）。

# --- 教師用（進行の実体・配布リンク・集計） ---
_teacher/
**/_teacher/

# --- 印刷プリント（問題・解答とも。原本の問題に由来する） ---
02_プリント/
**/02_プリント/

# --- 版元著作物・配布物 ---
*.pdf
*.docx
*.pptx
*.xlsx
*.zip

# --- 原本から抽出した画像 ---
_orig/
**/_orig/

# --- 作業中の一時物 ---
_work/
**/_work/
*.log
__pycache__/
**/__pycache__/
