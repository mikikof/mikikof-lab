# 第8回 — この回だけの台帳。
#
# manifest.toml には書かない。回ごとに別ファイルにしてあるのは、
# 別々のチャットで別の回を同時に作っても、同じファイルを奪い合わないため。
# 追加・改訂するときは、この回のファイルだけを触る。


no          = 8
slug        = "koushin-no-junjo"
theme       = "一段の中で、減らすのが先か増やすのが先か ― 確定的モデル"
nansho      = "一段の更新で、減らす計算と増やす計算の順序を入れ替えてしまう。日本語にするとどちらも同じように読めるのに、表を一段ずつ埋めると値がずれ、足りなくなる日が 1 日ずれ、必要な最初の量も変わる。式だけを見比べても違いが見えないので、表を最後まで埋めるまで気づかない"
nansho_type = "B"
date        = "未定"
status      = "done"
anchor      = { kind = "lec", id = 25 }

[artifacts]
explainer = "update-order/"    # 3ページのシリーズ（00 イチから図解 / 01 順序 / 02 時間間隔）＋回ハブ。
                               # 末尾の / がディレクトリの合図。公開 URL もこの名前で組まれる。
print     = "更新の順序"
quiz      = { id = "joho-update-order", set_index = 11 }
lecture   = 25
practice  = "04-15"
