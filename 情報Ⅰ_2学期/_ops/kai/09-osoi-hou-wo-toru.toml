# 第9回 — この回だけの台帳。
#
# manifest.toml には書かない。回ごとに別ファイルにしてあるのは、
# 別々のチャットで別の回を同時に作っても、同じファイルを奪い合わないため。
# 追加・改訂するときは、この回のファイルだけを触る。


no          = 9
slug        = "osoi-hou-wo-toru"
theme       = "始めるのは、二つのうち遅いほう ― 待ち行列"
nansho      = "待ち行列で、調理を始める時刻を「直前の客が終わった時刻」か「自分が来店した時刻」のどちらか一方だけで決めてしまう。遅いほうを取る、という一行が抜ける。直前の終了だけにすると、まだ来ていない客の料理を作り始めて待ち時間が負になる。来店だけにすると、二人分を同時に作ることになって行列ができない。どちらも表は最後まで埋まるので、負の数と重なりを数えるまで気づかない"
nansho_type = "A"
date        = "未定"
status      = "building"
anchor      = { kind = "lec", id = 26 }

[artifacts]
explainer = "queue-start-time/"   # 3ページのシリーズ（00 イチから図解 / 01 遅いほう / 02 乱数と平均）＋回ハブ。
                                  # 末尾の / がディレクトリの合図。公開 URL もこの名前で組まれる。
print     = "待ち行列の開始時刻"
quiz      = { id = "joho-queue-start", set_index = 12 }
lecture   = 26
practice  = "04-15"
