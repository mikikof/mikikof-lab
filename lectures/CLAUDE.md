# lectures/ — Claude Code 運用指示書(Interactive Lecture Lab)

このフォルダは、大学・高校の授業で使うインタラクティブなHTMLスライドサイトを量産する場所です。**単一HTMLファイル**で投影と復習の両方に使える教材を作ります。

ルートの `CLAUDE.md` からここに到達したら、本ファイルの指示に従って作業してください。

---

## 1. このツールの位置づけ

### ユーザー像
- **作り手**:幹生(平成国際大学 情報デザイン学部 教員)
- **投影者**:授業担当者
- **閲覧者**:受講学生(大学生、および情報Ⅰ共通テスト対策を受ける進学校の生徒)

### 成果物の特徴
- 単一 HTML ファイル(CSS・JS を全て内包、オフライン閲覧可)
- 授業中はプロジェクタ投影、授業後は生徒の自習復習に使える
- 学習ノートの項番(①②③…)と完全対応
- 最新の事例・ニュースを取り込んだ生きた教材
- **同一URLでPC(投影)とスマホ(復習)の両方に最適化**(ユニバーサル対応)
- **開閉式スライド目次ドロワー** + **インタラクション一括リセット** を標準装備

### 公開URL
- 授業サイトトップ:`https://mikikof.github.io/mikikof-lab/lectures/`
- 単元ページ:`https://mikikof.github.io/mikikof-lab/lectures/articles/{slug}/`

---

## 2. ディレクトリ構成

```
lectures/
├── CLAUDE.md                       本ファイル
├── index.html                      授業サイトトップ(単元一覧)
├── _source/                        原本素材。答えの正本
│   ├── 学習ノート_問題/            学習ノート 章別 Word(問題編)
│   ├── 高校情1学習ノート-問題PDF.pdf
│   └── 高校情1学習ノート-解答PDF.pdf  ← 答えはここを正本として扱う
│                                    ※ ベストフィット問題/解答は practices/_source/ に移動済み
├── skills/
│   └── interactive-lecture/        制作スキルパッケージ
│       ├── SKILL.md                技術仕様・制作手順
│       ├── template.html           空の骨組み(コピーして使う)
│       ├── components.md           部品カタログ(13種)
│       └── examples/
│           └── 07-information-security.html   凍結リファレンス
└── articles/
    └── {番号}-{スラッグ}/
        └── index.html              各単元の完成品
```

### 新規単元の命名規則
- 番号は学習ノート or 教科書の単元番号(07, 08, 09...)
- スラッグは英語ハイフン区切り(例:`information-security`, `computer-basics`)
- 配下は必ず `index.html`(GitHub Pages で `/articles/{slug}/` として公開される)

---

## 3. 作業着手前に必ず読むもの

新しい単元を作る・既存を修正するときは、**以下の順で必ず読んでから**コードに触る:

1. 本ファイル(このCLAUDE.md)— 制作哲学とチェックリスト
2. `skills/interactive-lecture/SKILL.md` — 技術仕様と落とし穴
3. `skills/interactive-lecture/components.md` — 部品カタログ
4. `skills/interactive-lecture/examples/` の最新完成品 — 生きた参照
5. **`_source/` の該当章 Word + 解答PDF の対応ページ** — 問題と答えの正本。これを読まずに書くと、答えの誤りや原本と異なる根拠フレームを書く事故が起きる(§4.8 参照)

これを読まずに書き始めると、毎回デザイン規約が揺れて量産の意味がなくなる。**skill と原本を読むのは念のためではなく必須**。

---

## 4. 制作哲学(これを外したらやり直し)

### 4.1 答えは必ず後出し
- 穴埋め、クイズ、実習問題の正解を先に見せない
- クリック・タップで展開する仕組みに統一する
- 「?」「クリックで確認」などのプレースホルダ表示を使う
- これを破ると**教材ではなく単なる解説資料になる**

### 4.2 問題は手応えのある難度
- 「使い回しはNG」「共用PCはNG」といった自明の問題は出さない
- 引っかけ、グレー判断、複数要素の組み合わせで判断を要する問題にする
- 対象は**共通テスト〜難関大受験生レベル**
- 「これなら教科書見れば分かる」問題は価値がない

### 4.3 実例を入れる
- 抽象的な説明だけでなく、現実の事件・ニュースで具体化する
- 情報セキュリティ系は **web_search で最新事例を確認**してから書く
- 事例は展開式で格納し、スライドを情報過多にしない

### 4.4 子供っぽい表現を避ける
- ❌「やってみよう」「頑張ろう」「みんなで考えてみよう」
- ❌ 過度な絵文字(💡 など機能的記号を除く)
- ✅ 中立的・プロフェッショナルな表現

### 4.5 対象属性の明示をしない
- 「東大受験生向け」等の特定属性明示はしない
- 「このサイトで扱う内容」「共通テストで問われる論点」のような内容ベース記述にする

### 4.6 学習ノートとの対応を明示
- 各スライドのラベルに「学習ノート ①」「実習 2」など対応箇所を表示
- 項番まで揃えることで、学生が手元のノートと照合しながら進められる

### 4.7 見出し・リード文のトーン
- スライド本文だけでなく、**`lectures/index.html` のヒーロー・単元カード説明・フッター**も同じ基準で書く
- ❌ 標語調:「授業の現場で、もう一度解ける教材を。」のような「〜を。」「〜に。」で止める体言止めヘッドライン
- ❌ 抽象メタファー:「立体化」「紐解く」「別のかたちで」「学びのハブ」
- ❌ 誇張形容:「深掘り」「詰め込む」「満載」「凝縮」「余すところなく」
- ❌ 対象属性の冠:「大学生・進学校の生徒向け」(4.5 と同方針)
- ✅ 事実ベースの平易な一文で、単元構成や使い方を淡々と述べる
- 詳細な禁止表現と Before/After はルート `CLAUDE.md` の「文章トーン」節と `essays/CLAUDE.md` §7-6 を参照

### 4.8 答えと根拠は原本(解答PDF)を正本として扱う
- lecture に載せる**正答・選択肢の○×・解説の根拠**はすべて `_source/高校情1学習ノート-解答PDF.pdf` の対応箇所を **直接 Read で開いて確認**する
- 「自分の知識でこうなるはず」「論理的にこうだろう」で書かない。原本の解説で使われている**根拠フレーム(条文・概念・論じ方)** をそのまま採用する
- 過去事故:著作権の例外規定問題で、原本は「著作権法35条(学校教育における複製)」フレームで判断していたのに、lecture は「私的使用」「引用」フレームで独自に判断。結論まで誤り、生徒から指摘を受けた(04-IP オの応援旗:正解「適切」を「不適切」と誤判定)
- 独自に4択や応用問題を追加する場合も、**原本の根拠フレームと矛盾しない**ことを確認すること
- agent(Explore など)に答えの監査を委譲した場合、「全件 ✓」報告を鵜呑みにせず、最低でも**指摘箇所と疑問の残る箇所は自分の目で原本PDFを Read して再確認**する

---

## 5. 技術的な制約

### 必須
- **単一HTMLファイル**で完結
- CSS・JS は全て内包
- Google Fonts の読み込みのみ外部 CDN として許可
- Safari(macOS / iPad)と Chrome で動作

### 禁止
- ❌ フレームワーク(React, Vue, Alpine.js 等)の導入
- ❌ localStorage / sessionStorage の使用
- ❌ 重いライブラリの CDN 読み込み
- ❌ ビルドステップが必要な構成(そのまま GitHub Pages で配信できない)

### ナビゲーション(標準装備)
- 矢印キー(← →)、スペース、PageUp/Down、Home/End
- タッチスワイプ(iPad / iPhone / Android)— 水平 40px 以上・800ms 以内で反応
- F キーで全画面切替
- M キー(または topbar 右端の `MENU` ボタン)で**スライド目次ドロワー**を開閉
- 進捗バー + ページ番号常時表示
- ESC でドロワーを閉じる

### スライド目次ドロワー(標準装備)
- セクション(SECTION 1〜N)ごとに自動でグルーピング
- 各項目に `PRACTICE` / `INTERACTIVE` / `REVIEW` などのタグを自動付与
- 現在スライドをハイライト、タップで任意のスライドへジャンプ
- 上部に **「インタラクション全リセット」** ボタン

### ユニバーサル対応(PC + スマホ)
- 同一URLで **PC(1100px+):横長スライド** / **タブレット(720–1100px):簡略化** / **スマホ(〜720px):縦に最適化された復習モード** を自動切り替え
- iPhone のセーフエリア(ホームインジケータ)に食い込まない
- 新規コンポーネントを追加するときは、必ず `@media (max-width: 720px)` も書く

詳細仕様は `skills/interactive-lecture/SKILL.md` を参照。

---

## 6. 標準ワークフロー(新規単元1本)

新しい授業単元の依頼を受けたら、以下の順で進める:

### [1] 素材の確認
- `_source/学習ノート_問題/` の対応章Word を python-docx でテキスト化し、項番・実習問題・POINT を洗い出す
- `_source/高校情1学習ノート-解答PDF.pdf` の対応ページ(章ごとに p.2〜17=1章、p.18〜33=2章…)を Read で開き、**正答と解説の根拠フレームを把握**する
- 対象単元番号・スラッグ・授業のコマ数を確認
- ユーザーから追加の学習ノート画像があればそれも参照

### [2] 構成案の提示と合意
- カバー範囲、スライド数、インタラクション要素を案として出す
- ユーザーに選択肢で確認(迷ったら聞く)
- **合意前にコードを書き始めない**

### [3] 事例・最新情報のリサーチ(該当する場合)
- web_search で該当トピックの最新事件を確認
- 3〜5件の事例候補をピックアップして組み込む

### [4] 実装
- `skills/interactive-lecture/template.html` をコピーして `articles/{番号}-{スラッグ}/index.html` に配置
- `components.md` から必要パーツをコピーして中身を組む
- デザイントークン(`SKILL.md` 参照)は絶対に変えない
- 問題の選択肢・正答・解説を書く時は、必ず [1] で開いた**解答PDFを傍に置きながら**書く(後でまとめて照合、ではなく書きながら照合)

### [5] 答えの照合(原本との突き合わせ) ★ 飛ばすと事故る
- lecture 内のすべての問題について、`_source/高校情1学習ノート-解答PDF.pdf` の対応箇所と以下を1問ずつ照合:
  - **正答(○×・選択記号)** が一致しているか
  - **解説の根拠フレーム**(条文・概念・論じ方)が原本と揃っているか — 結論だけ合っていても、根拠が原本と異なると別の問題で破綻する(§4.8 参照)
- 独自に追加した4択クイズや応用問題は、原本の根拠フレームと矛盾しないことを確認
- このステップの結果はユーザーへの報告に必ず含める(「解答PDF p.X〜Y と全問照合済み」のように)

### [6] 構文・動作チェック
- 中括弧バランス、scriptタグ、divタグの対応
- 既知の落とし穴(`SKILL.md` セクション8)に照らしてチェック

### [7] ユーザーへの報告
- 変更サマリを提示
- 「次はここが気になるかも」を先回りで伝える
- 投影してのフィードバック待ち

### [8] 完成後の凍結
- 完成版を `skills/interactive-lecture/examples/{番号}-{スラッグ}.html` にもコピー
- `lectures/index.html`(単元一覧)にエントリを追加

---

## 7. 納品前チェックリスト

完成と判断する前に、以下を全てクリアすること:

### 内容
- [ ] 学習ノートの全項番 + 全実習が網羅されている
- [ ] **全問の正答・解説の根拠フレームを `_source/高校情1学習ノート-解答PDF.pdf` と1問ずつ照合済み**(§6 [5] のステップを完了)
- [ ] 答えは全てクリック/タップで後出し展開(語群・一覧でも正解を先に色付けしていない)
- [ ] 事例セクションがある場合、最新(過去1〜3年)の事件が含まれている
- [ ] 禁止表現(「やってみよう」「東大受験生」等)が残っていない

### デザイン・書体
- [ ] 紺・青系のデザイントークンが守られている
- [ ] セクション仕切りが画面中央に表示される
- [ ] 本文・連続文にNoto Serif JP(明朝)を使っていない(見出しのみ明朝)
- [ ] 本文は 16px / 500 以上、問題文は 16px / 500 以上で、スマホでも視認できる

### ナビゲーション・機能
- [ ] 上下バーとコンテンツが衝突していない(特にスマホのbottombar)
- [ ] 矢印キー・スペース・タッチスワイプ・F・M キー全て動作
- [ ] MENU ドロワーが開閉し、目次から任意スライドに飛べる
- [ ] インタラクション一括リセットが動作する(単元固有は `onResetHook` に登録済み)
- [ ] 進捗バー・ページ番号が正しい
- [ ] 分類ゲーム等の配置系は一方通行ではなく戻せる(undo可能)

### レスポンシブ(ユニバーサル)
- [ ] 1100px / 720px / 380px / landscape phone の各幅で崩れない
- [ ] `<meta viewport-fit=cover>` が入っている
- [ ] bottombar/slide が `env(safe-area-inset-bottom)` で iPhone ホームインジケータを避けている

### リリース
- [ ] `examples/` に凍結コピーを配置
- [ ] `lectures/index.html` にエントリ追加

---

## 8. Git 運用

コミットプレフィックスは `lectures:` を使う(ルート CLAUDE.md の規約に準拠):

```
lectures: 07-information-security を新規追加
lectures: パスワード管理の選択肢を難化
lectures: SKILL.md に落とし穴を追記
```

他ツール(`essays/`)と混在させない。

---

## 9. 困ったとき

- 実装で迷ったら `examples/` の最新完成品を読む
- デザインで迷ったら `SKILL.md` の Design Tokens セクション
- 挙動バグに遭遇したら `SKILL.md` 末尾の「既知の落とし穴」
- 学習ノートとの対応で迷ったらユーザーに聞く(勝手に省略しない)
- **問題の正答や解説の根拠で迷ったら独自推論せず、必ず `_source/高校情1学習ノート-解答PDF.pdf` の対応ページを Read で開いて確認**(§4.8)
- それでも判断つかない場合は作業を止めてユーザーに確認
